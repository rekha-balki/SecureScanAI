fastapi
uvicorn[standard]
pydantic
pydantic-settings
motor
pymongo
redis
aiokafka
structlog
python-jose[cryptography]
bcrypt>=4.1,<5
python-multipart
reportlab
openpyxl
httpx
email-validator
python-dotenv
motor>=3.7.1
# Optional: enables JS-rendered link discovery (see
# app/domains/scan/services/js_rendering_service.py). Not required for
# the rest of the platform; scanner_defaults.enable_js_rendering stays
# off by default.
# playwright>=1.45

# Optional: enables Shodan-style favicon hash matching in
# site_checks.py's favicon fingerprinting check. Falls back to
# MD5/SHA-256 only if not installed.
# mmh3>=4.1
